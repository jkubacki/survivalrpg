
%h3 Place
%strong.name
  = @place.name
  %span{ style: 'font-size: small' }= "(#{@place.user.email})"
.description
  = @place.description

%h3 Encounters
- @encounters.each do |encounter|
  %strong.name
    = encounter.name
    %span{ style: 'font-size: small' }= "(#{encounter.user.email})"
  .description
    = encounter.description

.reward-actions{ style: 'margin-top: 20px;' }
  = link_to 'Reward', games_reward_path(risk: @risk), class: 'btn btn-success'
  = link_to 'No reward', games_new_path, class: 'btn btn-danger'

